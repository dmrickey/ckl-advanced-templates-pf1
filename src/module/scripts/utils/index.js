import getToken from './get-token-from-item';
import ifDebug from './if-debug';
import { localize, tLocalize } from './localize';
import targetTokens from './target-tokens-in-template';

export {
    getToken,
    ifDebug,
    localize,
    tLocalize,
    targetTokens,
};
